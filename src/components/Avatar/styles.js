import { StyleSheet } from 'react-native';
import { darkGrey, pureBlack } from '../../styles/colors';

export default StyleSheet.create({
    container: {
        borderWidth: 1,
        borderColor: pureBlack
    }
});
